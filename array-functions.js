const users = [
  { name: "Zeyneb", age: 25 },
  { name: "Ayşe", age: 24 },
  { name: "Zeyneb", age: 34 },
];

/*
    push
    map
    find
    filter
    some
    every
    includes
*/

//push
// users.push("Eda");
// users.push("Ela");

// console.log(users);

//map

// for (let i = 0; i<users.length;i++)
// {
//     console.log(users[i]);
// }

// users.map((item) => {
//   console.log(item);
// });

//find

// const result = users.find((item) => item.name === "Rumeysa");
// console.log(result);

//filter

// const filtered = users.filter((item) => item.name === "Zeyneb");
// console.log(filtered);

//some (true or false)

// const some = users.some((item) => item.age == 24);
// console.log(some);

//every

// const every = users.every((item) => item.age > 24);
// console.log(every);

//includes

// const meyveler = ["elma", "armut", "portakal"];
// const isIncluded = meyveler.includes("elma");
// console.log(isIncluded);
